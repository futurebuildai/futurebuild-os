{
  "name": "futurebuild-root",
  "version": "2.1.0-beta",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "FutureBuild - AI-Powered Construction Scheduling Platform",
  "scripts": {
    "dev": "concurrently \"go run ./cmd/api\" \"npm --prefix frontend run dev\"",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test && go test ./...",
    "shadow:scaffold": "node scripts/shadow/scaffold.js",
    "shadow:check": "node scripts/shadow/check.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}